<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:rabbit="http://www.springframework.org/schema/rabbit"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
		http://www.springframework.org/schema/beans/spring-beans.xsd


		http://www.springframework.org/schema/rabbit
        http://www.springframework.org/schema/rabbit/spring-rabbit.xsd">

    <!-- 连接服务配置  -->
    <rabbit:connection-factory id="connectionFactory" host="127.0.0.1" username="guest"
                               password="guest" port="5672" virtual-host="/" />


    <rabbit:admin connection-factory="connectionFactory" />

    <!--direct: exchange queue binging key 绑定 -->
    <!-- queue 队列声明-->
    <rabbit:queue id="direct_queue" durable="false" auto-delete="true" exclusive="false" name="direct_queue"/>

    <!-- exchange -->
    <rabbit:direct-exchange name="direct_exchange" durable="true" auto-delete="false" id="direct_exchange">
        <rabbit:bindings>
            <rabbit:binding queue="direct_queue"/>
        </rabbit:bindings>
    </rabbit:direct-exchange>


    <!--topic: exchange queue binging key 绑定 -->

  <!--  <rabbit:topic-exchange name="myexchange1" durable="true" auto-delete="false" id="myexchange1">
        <rabbit:bindings>
            <rabbit:binding queue="queue_one" pattern="foo.*"></rabbit:binding>
        </rabbit:bindings>
    </rabbit:topic-exchange>-->


    <!--fanout: exchange queue binging key 绑定 -->
    <!--<rabbit:fanout-exchange name="myexchange2"   durable="true" auto-delete="false" id="myexchange1">-->
    <!--<rabbit:bindings>-->
    <!--<rabbit:binding queue="queue_one"></rabbit:binding>-->
    <!--</rabbit:bindings>-->
    <!--</rabbit:fanout-exchange>-->

    <!-- 消息转换器 -->
    <bean id="messageConverter" class="org.springframework.amqp.support.converter.SimpleMessageConverter" />


    <!-- spring amqp默认的是jackson 的一个插件,目的将生产者生产的数据转换为json存入消息队列-->
    <!--<bean id="jsonMessageConverter" class="com.rayootech.rabbitmq.demo.springamqp.FastJsonMessageConverter" />-->

    <!-- spring template声明-->
    <!--direct-->
    <!--<rabbit:template exchange="my-mq-exchange" id="amqpTemplate"  connection-factory="connectionFactory" message-converter="jsonMessageConverter"/>-->
    <!--topic-->

    <rabbit:template exchange="direct_exchange" id="amqpTemplate" connection-factory="connectionFactory"
                     message-converter="messageConverter" routing-key="*"/>
    <!--fanout-->
    <!-- <rabbit:template exchange="myexchange2" id="amqpTemplate"  connection-factory="connectionFactory" message-converter="jsonMessageConverter"/>-->

</beans>